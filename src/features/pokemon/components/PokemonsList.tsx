export const PokemonsList = () => {
  return (
    <>
      <div>Pokemons List</div>

      {/* {pokemonsList.results.map((result: Result) => (
        <PokemonCard key={result.name} name={result.name} />
      ))} */}
    </>
  );
};
